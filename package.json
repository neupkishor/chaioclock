{
  "name": "chaioclock",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "rm -rf core && rm -rf neup.core && git clone https://github.com/neupgroup/neup.core && mv neup.core core && rm -rf logica && rm -rf neup.logica && git clone https://github.com/neupgroup/neup.logica && mv neup.logica logica && cd core && rm -rf database",

    "prebuild": "npm run setup && npx prisma generate",
    "predev": "npm run setup",

    "dev": "next dev --port 2424 --experimental-https",
    "build": "next build",
    "start": "next start --port 2424",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.3.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
